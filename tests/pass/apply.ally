module list {
	def apply(f, val) {
		val > f > return;
	}	


	
	def square1(x) {
		(x, x) > mul > return;		
	}
	
	def square2(x) {
		return < mul < (x, x);
	}

	def square3(x) {
		|= mul
		|< (x, x)
		|> return;
	}

	def square4(x) {
		return < |= mul
		         |< x
		         |< x;
	}

}
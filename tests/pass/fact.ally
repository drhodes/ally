module math {
    def apply (func f, var x) {
        |= ap (f, x)
        |> return;
    }

    def fact (var n, func f) {
        var tmp = 1;
        
        min1 <> |= n > mul <> tmp > dup > return
                |> |= gt1
                   |+> min1
                   |-> dup
                   ;
    }

    def main () {
        var msg = "asdf";
        sys.out < msg;
    }
}
